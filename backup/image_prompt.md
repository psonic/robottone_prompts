# Default prompt to use when no user prompt is provided
default_prompt=A high-quality cypherpunk hacker aesthetic image

# Style suffix to append to every prompt (user-provided or default)
style_suffix=, pixel art lo fi cat
